# 폴앤다니엘기독학교(PDCS) 홈페이지 프로젝트

## 폰트 정책 (모든 홈페이지 작업에 예외 없이 적용)

클라이언트 지시: **저작권 문제가 없는 폰트만 사용한다.**

### 허용 — 이 목록 안에서만 고른다
- **SIL Open Font License 1.1** 폰트
  - Pretendard / Pretendard Variable (한글 본문·제목 기본값)
  - Noto Sans KR, Noto Serif KR, 본고딕(Source Han Sans), 본명조
  - Google Fonts 전 서체 (대부분 OFL, 일부 Apache 2.0 — 둘 다 허용)
- **Apache License 2.0** 폰트

### 금지 — 라이선스 확인 없이는 절대 사용하지 않는다
- 유료 폰트 (산돌, 윤서체, 아모레퍼시픽 등 상용 서체)
- "무료 배포"라고만 적힌 기업 폰트 — 무료 사용과 웹폰트 임베딩은 별개 권리다.
  배포처 라이선스 원문에서 **웹폰트 임베딩 허용 여부**를 확인하기 전에는 쓰지 않는다
- 출처 불명 폰트 파일 (블로그·자료실·토렌트 배포본)
- 클라이언트가 준 디자인 시안(PSD/AI/PPT)에 박혀 있는 폰트를 확인 없이 그대로 사용

### 작업 규칙
1. 폰트 파일은 **공식 배포처에서만** 받는다 (공식 npm 패키지, 공식 GitHub 릴리스, Google Fonts)
2. 웹폰트는 외부 CDN 링크가 아니라 **자체 호스팅**한다. CDN 중단 시 글씨가 통째로 깨지고,
   해외 CDN은 방문자 IP가 외부로 나가는 문제도 있다
3. 이미지·배너에 얹는 글자도 동일한 라이선스 규칙을 적용한다. "그림이니까 괜찮다"는 성립하지 않는다
4. 납품 시 **폰트 라이선스 증빙**을 함께 남긴다: 폰트명, 버전, 라이선스 종류, 공식 출처 URL, 확인 일자
5. 라이선스를 직접 확인하지 못했으면 "확인하지 못했다"고 말한다. 추정해서 안전하다고 하지 않는다

### 현재 이 프로젝트에서 쓰는 폰트
| 폰트 | 버전 | 용도 | 라이선스 | 출처 |
|---|---|---|---|---|
| Pretendard Variable | 1.3.9 | 한글·영문 전체 | SIL OFL 1.1 | 공식 npm 패키지 `pretendard` |

**이 한 벌뿐이다.** `assets/fonts/`에 `PretendardVariable.woff2` 하나만 있고,
CSS 전체에 다른 서체 선언이 없다(grep으로 확인). 한때 Cormorant Garamond를
영문 장식용으로 검토했으나 파일을 포함하지 않았으므로 표에서 뺐다.
쓰지 않는 폰트를 증빙표에 적으면 납품 증빙 자체가 성립하지 않는다.

영문 장식 서체를 쓰려면 파일을 받아 `assets/fonts/`에 넣고 이 표를 먼저 갱신한다.
파일 없이 `font-family`만 선언하면 시스템 Times로 떨어져 Pretendard 옆에서 값싸 보인다.

---

## 기술 원칙 (클라이언트 지시, 예외 없음)

**GitHub 저장소와 GitHub Pages만으로 모든 작업이 끝나야 한다.**

- 서버, 데이터베이스, 배포 플랫폼을 새로 붙이지 않는다
  (Supabase, Vercel, Netlify, Firebase 등 일체 사용 금지)
- 빌드 도구도 쓰지 않는다. 파일을 그대로 올리면 그대로 보이는 구조를 유지한다
- 외부 서비스가 필요해 보이는 기능은 **정적으로 대체하거나 빼고**, 왜 그랬는지 말한다

### 배포 전 반드시 할 것 (순서대로, 예외 없이)

```
./bump.sh          # 스타일·스크립트 주소에 내용 해시를 붙인다
python3 check.py   # 통과하지 못하면 배포하지 않는다
```

`check.py`가 잡는 것 — **한 번이라도 눈으로 놓쳐 배포된 적이 있는 항목은 전부 들어간다**
- 학교가 쓰지 말라고 한 말(School of Tomorrow / IGNITIA / ACSI / 140개국)
- 제작용 흔적(`［ ］`, TODO), 눌러도 아무 일 없는 링크, 422KB 원본 로고
- 4페이지 × 7뷰포트 가로 넘침
- **글자와 배경의 대비** — 작은 글자 4.5:1, 큰 글자 3:1
- **모바일 메뉴를 실제로 열어** 항목이 보이는지
  (투명 헤더의 흰 글자 규칙 때문에 메뉴 글자가 흰 배경에 묻혀 배포된 적이 있다)
- JS 오류, 404

새로 발견한 실수는 그때마다 `check.py`에 넣는다. 사람이 조심하는 것으로는 막히지 않는다.

`./bump.sh` 는 스타일·스크립트 주소에 파일 내용 해시를 붙인다. 스타일·스크립트 주소에 파일 내용 해시를 붙여,
브라우저가 옛 파일을 계속 쓰는 것을 막는다. 이걸 빠뜨리면 고친 내용이
방문자 화면에 반영되지 않고, 고쳤는데 안 고쳐졌다는 오해가 생긴다.

**배포본은 이 작업 환경에서 열어볼 수 없다**(intoedu.github.io 접속 차단).
로컬 검증만으로 "됐다"고 말하지 말고, 확인은 클라이언트에게 부탁한다.

### 이 원칙을 지킨 방식
| 기능 | 보통의 방법 | 이 프로젝트 |
|---|---|---|
| 상담 신청 | 서버로 전송 · DB 저장 | 입력 내용을 **문자(모바일) / 메일(PC)** 로 자동 작성. 서버 없음 |
| 지도 | 지도 API 키 발급 | **구글지도 iframe** + 카카오맵 · 구글지도 길찾기 링크. 키 불필요 |
| 공지사항 | 게시판 · 관리자 페이지 | `index.html`의 카드를 직접 고친다. 고치고 push 하면 반영 |
| 영상 | 영상 호스팅 | 유튜브 iframe |
| 폰트 | 폰트 CDN | 저장소에 woff2 직접 포함 |

## 브랜드 기준
엠블럼에서 추출한 실제 색상값이다. 임의로 바꾸지 않는다.

| 이름 | 값 | 용도 |
|---|---|---|
| Deep Navy | `#0E2245` | 기본 배경·제목 |
| Navy | `#16305C` | 보조 |
| Gold | `#C89B3C` | 강조·영문 레터링 |
| Signal Red | `#B3242A` | CTA 전용. **전체 면적 3% 이하** |
| Paper | `#FAF8F3` | 본문 배경 |
| Sand | `#EFE9DC` | 구분 배경 |
| Muted | `#5A6275` | 보조 텍스트 |
| Ink | `#14161A` | 본문 텍스트 |

- 클라이언트 초기 요청은 "파랑·빨강"이었으나, 엠블럼 실측 결과 네이비+골드가 주색이고
  빨강은 성경 책갈피에만 소량이다. 빨강을 동등 비중으로 쓰면 학교가 아니라 스포츠 구단처럼 보인다.
  네이비+골드 주조, 빨강은 CTA 한 곳에만 — 이 방침을 클라이언트에게 설명하고 진행 중이다.

## 로고 (확정 — 변경 제안하지 말 것)

**원형 엠블럼이 학교 공식 메인 로고다.** 기존 홈페이지(pdsot.com)가 쓰던
방패형 + PDCS 워드마크로 바꾸자고 제안했으나, 학교에서 원형 엠블럼이
이제 메인이라 바꾸기 어렵다고 확인해 주었다. 이 결정을 다시 꺼내지 않는다.

웹에서 쓰는 방식:
| 파일 | 크기 | 용량 | 용도 |
|---|---|---|---|
| `logo-96.png` | 96px | 4.8KB | 헤더·푸터 1x |
| `logo-192.png` | 192px | 11.2KB | 헤더·푸터 2x (고해상도 화면) |
| `apple-touch-icon.png` | 180px | 10.4KB | 홈 화면 바로가기 |
| `favicon-64.png` | 64px | 2.8KB | 브라우저 탭 |
| `logo.png` | 720px | 422KB | **원본 보관용. 화면에 직접 쓰지 않는다** |

- 원본 422KB를 50px로 줄여 쓰면 첫 화면 로딩 경로를 그대로 막는다. `srcset`으로 건다
- 로고 이미지 자체를 변형하지 않는다(색 반전·크롭·재배치 금지).
  어두운 배경 대비는 `drop-shadow`로만 보강한다
- 로고를 고치면 위 네 파일을 다시 만들고 이 표를 갱신한다

## 학교 정보 (확정)
- 교명: 폴앤다니엘기독학교 / Paul and Daniel Christian School (PDCS)
- 교장: 김남주 · 설립 2010년 · 표어: For Christ For All Nations
- 주소: 충청남도 아산시 음봉면 아산온천로 148-39
- 유선: 041-425-0085 (대표 · **사업 번호**) / 041-425-0096 / 042-623-7067
  - 학교가 대표번호를 0096 → 0085 로 바꿔 달라고 했다. **사업 번호로 통일하려는 것**이다.
    화면의 "전화하기"·푸터·유틸바·tel: 링크는 전부 0085 다 (check.py가 검사한다)
- 무선: 010-9665-7391 / 010-6628-8290 (부재 시 문자)
- 운영시간: 평일 09:00 – 17:00
- 기존 홈페이지: pdsot.com (임대형 솔루션, 구 옥천 주소가 남아 있음)

### 학교가 말한 "글 색상을 검은색으로" — 무슨 뜻이었는지 확인됨
**모바일 메뉴 글자가 안 보이던 것**을 가리킨 말이다(학교 확인 완료).
본문 글자를 전부 검게 바꾸라는 뜻이 **아니다**. 본문은 Ink `#14161A`,
보조는 Muted `#5A6275` 그대로 둔다. 이 요청을 다시 꺼내 디자인을 건드리지 않는다.

## 화면에 내보내면 안 되는 것

**제작용 플레이스홀더를 공개 화면에 남기지 않는다.**
`［ 학교 확인 후 채웁니다 ］` 같은 표시는 학부모에게 "학교가 숨기고 있거나
학교 자신도 모른다"로 읽힌다. 둘 다 전화를 안 걸게 만든다.
자료가 없으면 그 요소를 **빼거나**, 지금 사실로 말할 수 있는 내용으로 **바꾼다**.
점선 빈 상자로 없는 것을 광고하지 않는다.

**답할 수 없는 질문을 목록으로 내걸지 않는다.**
답 없는 FAQ 아코디언보다, 질문 목록 + "전화로 정확히 안내드립니다"가 정직하고 유용하다.

**눌러도 아무 일이 없는 링크를 두지 않는다.**
`href="#"`는 배포 전 전부 없앤다. 실제 주소가 없는 항목은 목록에서 뺀다.

## 쓰면 안 되는 말 (학교 지시)

**교육과정에 특정 교재·프로그램 이름을 넣지 않는다.**
- `School of Tomorrow` / `S.O.T` — 사용 금지
- `IGNITIA` — 사용 금지
- `ACSI` 가입교·인증 표기 — 사용 금지
- "140여 개국이 쓰는" 같은 교재 보급 실적 — 사용 금지

학교가 명시적으로 지워 달라고 요청했다. 2차 자료(언론·위키)에 나와 있더라도 쓰지 않는다.
교육과정은 **학교가 하는 일**로 설명한다 — 각자의 진도, 곁에서 지도, 하루 세 번 말씀.

**핵심 포인트는 「대안교육기관」이다.**
학교가 강조해 달라고 한 지점이다. 정규 학교를 흉내 내는 표현 대신,
대안교육기관으로서 무엇이 다른지를 앞에 세운다.

## 확인 필요 — 사실로 단정해서 쓰지 말 것
아래는 2차 자료(언론·위키)에서 본 내용이다. 학교 공식 확인 전에는 홈페이지에 단정 표기하지 않는다.
- **학력 인정 여부와 상급학교 진학 경로** — 미인가 대안교육기관으로 분류된 자료가 있다.
  학부모 분쟁의 최대 리스크이므로 사실 그대로 명확히 표기한다
- 실제 일과표 (현재 시안의 시간은 배치 예시)
- 입학 전형 일정·제출 서류·학비

## 개인정보·초상권
- 문의 양식은 개인정보 수집에 해당한다. 개인정보처리방침 페이지와 수집 동의 절차가 필수다
- 학생 얼굴이 나오는 사진은 학부모 동의를 받은 컷만 사용한다
- 교사 사진·약력은 본인 동의를 받은 분만 노출하는 구조로 만든다

## 작업 산출물
- 디자인 시안: https://claude.ai/artifact/TF2sG65xHQLue1zA9yyosX (Design 캔버스, 비공개)
  - 브랜드 시스템 / 메인 PC / 메인 모바일 / 학교소개 / 입학·문의 / 움직임 설계
